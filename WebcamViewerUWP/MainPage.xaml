<Page
    x:Class="WebcamViewerUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebcamViewerUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    winui:BackdropMaterial.ApplyToRootOrPageBackground="True"
    Loaded="Page_Loaded">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition x:Name="titlebar_rowdef" Height="Auto" d:Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="frame_container" Grid.Row="0" Grid.RowSpan="2"/>

        <!-- Titlebar -->
        <Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Padding="14,0">
                <TextBlock Text="&#xe156;" FontFamily="Segoe MDL2 Assets"
                           VerticalAlignment="Center" Padding="0,0,12,0"/>
                <TextBlock Text="Webcam Viewer" VerticalAlignment="Center" FontSize="12" />
            </StackPanel>
        </Grid>

    </Grid>
</Page>
